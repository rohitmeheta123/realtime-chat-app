// Placeholder message controller for future message API endpoints

export const getMessages = async (req, res) => {
  res.status(501).json({ success: false, message: 'Message endpoints not implemented yet' });
};

export const sendMessage = async (req, res) => {
  res.status(501).json({ success: false, message: 'Message endpoints not implemented yet' });
};
