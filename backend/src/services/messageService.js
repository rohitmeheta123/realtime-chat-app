// Placeholder message service for future database operations & business logic

export const fetchAllMessages = async () => {
  return [];
};

export const createNewMessage = async (sender, message) => {
  return null;
};
